{
  "name": "uaos-studio-removed",
  "private": true,
  "version": "0.0.0",
  "description": "Sanity Studio removed from UAOS. Kept as a placeholder so old scripts fail clearly.",
  "scripts": {
    "dev": "node -e \"console.error('Sanity Studio removed. Use /admin'); process.exit(1)\"",
    "build": "node -e \"console.error('Sanity Studio removed. Use /admin'); process.exit(1)\"",
    "deploy": "node -e \"console.error('Sanity Studio removed. Use /admin'); process.exit(1)\""
  }
}

# Sanity setup (removed)

Sanity CMS has been removed from UAOS.

Use instead:

- `docs/OPS_CHECKLIST.md` — cutover, MFA, Neon/Blob/Turnstile
- `docs/RUNBOOK.md` — restore, offboarding, incidents
- `/admin` — applications + content editors
- `npm run db:migrate` / `npm run db:seed` / `npm run admin:create`

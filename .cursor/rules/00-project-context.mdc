---
description: "Постоянный контекст, критические области и границы проекта UAOS."
alwaysApply: true
---

# UAOS: контекст проекта

- Продукт: публичный сайт ГС «Українська Асоціація Професійної Безпеки» с каталогом участников, событиями, новостями, документами и формой вступления.
- Клиент: React 19 + Vite 6.
- CMS/данные: Neon Postgres + защищённая админка `/admin`; медиа — Vercel Blob.
- Hosting/API: Vercel, включая `POST /api/join`, `/api/public/*`, `/api/admin/*`.
- Языки: `uk` по умолчанию и `en` (также UI-локали de/es/kk/fr).
- Темы: `light` и `dark`.
- Стадия: разработка.
- `studio/` — placeholder «Sanity removed», не рабочий CMS.

## Основные сценарии

- просмотр информации об ассоциации;
- каталог и профили участников;
- события, новости и документы;
- подача заявки на вступление;
- cookie consent и подключение analytics;
- редактирование контента и заявок через `/admin`.

## Критические области

- заявки на вступление, содержащие PII и consent trail (`applications`, `consents`);
- `DATABASE_URL`, `SESSION_SECRET`, `MFA_ENC_KEY`, Blob/Turnstile/Brevo и другие секреты окружения;
- серверная валидация и rate limit формы вступления;
- выбор между Neon API и локальным seed (только DEV fallback);
- analytics только после согласия пользователя.

## Источники истины

- фактическая реализация: исходный код, manifest, lock-файлы и конфигурация;
- структура и миграции данных: `db/migrations/*`, `api/_lib/contentRepo.ts`, `api/_lib/applicationsRepo.ts`;
- загрузка и преобразование контента: `src/data/*`, `src/lib/contentApi.ts`, `src/lib/contentGuards.ts`;
- форма вступления: `src/lib/joinRequests.ts`, `api/join.ts`, `api/_lib/*`;
- UI-тексты: `src/data/translations/*`;
- дизайн: `src/index.css`, существующие компоненты и `uaos-design-system.mdc`;
- архитектурная навигация: актуальный `CODE_MAP.md`.

Память модели и общие примеры не являются источником истины о текущем проекте.

## Границы

- Не добавляй бизнес-требования и не меняй назначение продукта самостоятельно.
- Не меняй поддерживаемый стек, платформы или архитектурный стиль без отдельной задачи.
- Commit, push, PR, deploy, seed, массовые записи и работа с production Neon/Blob требуют прямого разрешения.
- При конфликте спецификации и кода сначала обозначь расхождение; не исправляй его молча.
